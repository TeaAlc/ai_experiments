langchain-core
# we need ollama with
# - bge-m3 (embeddings)
# - gpt-oss:20b (llm)
langchain
langchain-ollama
langchain-openai

#langchain-core  # For the InMemoryVectorStore
#numpy           # needed for the vector store search

#jinja2          # For the RAG Document processing

rich            # For better terminal outputs
grandalf        # For printing langchain graphs as Ascii Art


# -- Now the part for the sound --
numpy
sounddevice

# sudo apt-get -qq -y install espeak-ng > /dev/null 2>&1
kokoro

# Kokora runtime dependencies
spacy==3.8.0
en-core-web-sm==3.8.0
